package com.twitter.elephantbird.mapreduce.input;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

public class TestLzoProtobufBlockInputFormat {

  @Test
  public void testCreation() {
    assertTrue("we need to write a real test", true);
  /*
   *   LzoDeviceInputFormat a = new LzoDeviceInputFormat();

    System.out.println("a's class is " + a.getClass());

    LzoStatusInputFormat b = new LzoStatusInputFormat();
    System.out.println("b's class is " + b.getClass());

    Message m = Protobufs.instantiateFromClassName("com.twitter.elephantbird.data.proto.Tables.Device");
    System.out.println("m's classname is " + m.getClass() + " and it looks like[ " + m + " ]");

    Device m2 = Protobufs.<Device>instantiateFromClass(Device.class);
    System.out.println("m2's classname is " + m2.getClass());

    Device m3 = Protobufs.<Device>parseFrom(Device.class, new byte[] {});
    System.out.println("m3's classname is " + m3.getClass());
*/
  }
}
